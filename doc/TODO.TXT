Items to describe:

1.  compact command move all files AND defrag fragmented if possible
2.  currently compact don't applied to FAT volumes because directories on them
    isn't moveable.
3.  gui app hot keys:

    Ctrl + A - analyse
    Ctrl + C - compact
    Ctrl + D - defrag
    Ctrl + F - show fragmented
    Ctrl + P - pause
    Ctrl + S - stop
    Ctrl + R - rescan drives
    Ctrl + T - settings
    F1       - about
    Pause    - pause

4.  Pause and Stop actions are equal. You can analyse volume once and then
    defrag and/or compact them few times.
5.  FAQ must be rewritten in good style
6.  Readme.txt must be rewritten in good style
7.  To developers: program has clear and easy to understand code.
8.  Full support for compressed files
9.  Console command line options description. And nice appearance description.
10. files with size over limit are shown with dark color
11. full description of filter using. patterns are applied to full path. So \temp
    include either temporary files or internet explorer cache.
12. in report - all excluded files are marked with black color
13. settings - report encoding - ASCII is useful for Internet explorer and 
    utf-16 for mozilla firefox (better for chinese and japanese)
14. They are versions for all of the supported processor architectures for x64 
    Windows.
15. progress bar disappears when you select other drive: it's normal behaviour
16. can analyse and defrag floppies (isn't useful but ...)
17. report - comment column:
    [OVR] - file is over limit
    [DIR] - is directory
    [CMP] - compressed file
18. about report - small size but very useful [should be in future] - generated 
    by driver very fast.
19. filter: ignore files greater than: n [Kb | Mb | Gb | Tb]
20. Debug print: normal to view useful messages about analyse / defrag progress;
    detailed to send to an author when error encountered.
21. Virus scanner must be stopped.
